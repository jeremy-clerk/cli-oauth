# Clerk OAuth Configuration
CLERK_OAUTH_CLIENT_ID= # Optional - if provided, will use client credentials flow
CLERK_OAUTH_CLIENT_SECRET=  # Optional - if provided, will use client credentials flow
CLERK_OAUTH_DOMAIN=clerk.your-app.com
CLERK_OAUTH_REDIRECT_URI=http://localhost:3000/callback